import { population } from './demographic';

const VACCINE_PER_WEEK = 480000 
const VACCINE_PER_DAY = VACCINE_PER_WEEK/7;

// function popAheadOfAge(age: number): number {
//     if (age == null) {
//         return -1;
//     }

//     const popAhead = 0;
//     for ( let iterAge = population.length, iterAge > 0, iterAge--)
//         if (iterpopulation.reverse().forEach((pop, age) => {
//         const ageIter

//     })
// }